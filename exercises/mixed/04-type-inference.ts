/**
 * Урок 4: Type Annotations vs Type Inference
 */

// 1. Удали ИЗБЫТОЧНЫЕ аннотации типов (где TS сам догадается)
let myCity: string = "Moscow";
let myAge: number = 30;
let hasCar: boolean = false;

// 2. Здесь аннотация НЕОБХОДИМА. Без неё тип вывелся как 'any', а это плохо.
// Добавь аннотацию так, чтобы в переменную 'price' можно было положить только число.
let price;
price = 100;
price = 4.20;

// 3. Исправь аннотацию.
// TypeScript вывел тип 'number' для переменной 'id',
// но мы хотим хранить И числа И строки.
// Используй Union Type: number | string
let id: number = 123;
id = "a1-b2"; // Сейчас тут ошибка — исправь строку выше

// 4. Посмотри на этот массив.
// Какой тип выведет для него TypeScript автоматически?
// Напиши свой ответ в комментарии ниже.
let items = ["apple", 10, true];
// Ответ: items — это массив типа ...

console.log("Урок 4 в процессе!");
